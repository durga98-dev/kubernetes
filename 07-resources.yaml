kind: Pod
apiVersion: v1
metadata:
  name: resources
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      # soft limit (min)
      requests:
        memory: "68Mi"
        cpu: "100m"
      # hard limit (max)
      limits:
        memory: "128Mi"
        cpu: "250m"
